# 퀘스트형 온보딩 API 연동 작업 계획

## 1. 목적

- 현재 프론트에 하드코딩/로컬 계산 기반으로 구현된 퀘스트형 온보딩을 서버 API 기준 동작으로 전환한다.
- `GET /api/onboarding/status`, `GET /api/onboarding/quests`를 기준으로 온보딩 진입, 홈 배지, 퀘스트 목록/진행률, 대기 상태를 일관되게 맞춘다.
- API 명세로 대체 가능한 기존 프론트 로직은 제거하거나 축소한다.

## 2. 완성조건

- 닉네임 저장 후 첫 번째 퀘스트 미완료 사용자는 `/quest`로 진입한다.
- 닉네임 저장 후 첫 번째 퀘스트 완료 사용자는 `/home`으로 진입한다.
- 홈 헤더의 퀘스트 배지가 `onboarding/status.hasNewQuest`와 동기화된다.
- 퀘스트 진입점은 `/quest` 하나로 통합되고, 서버 상태에 따라 진행 화면 또는 대기 화면으로 분기된다.
- 대기 상태와 온보딩 완료 후 퀘스트 진입 시 기존 `QuestTemplateScreen` UI를 유지하면서 이동만 막고 `alert`로 안내한다.
- 퀘스트 진행률과 완료 여부는 서버 응답 기준으로 표시된다.
- 옷장 등록/옷 등록 후 온보딩 관련 쿼리가 무효화되어 화면이 최신 상태로 갱신된다.
- 각 파일 수정 배치가 끝날 때마다 subagent 리뷰를 받고, 피드백을 반영한 뒤 다음 배치로 진행한다.

## 3. 범위

## 3.1 작업 주의사항

- 이미 구현된 UI는 수정하지 않는다.
- 여기서 UI는 사용자가 실제 화면에서 보는 레이아웃, 스타일, 간격, 문구 배치, 아이콘/이미지 구성, 버튼 위치와 같은 가시 요소를 의미한다.
- 수정 가능한 범위는 화면에 보이지 않는 로직, API 연동, 상태 분기, 데이터 바인딩, 라우팅, 쿼리 무효화, 에러 처리로 한정한다.
- 기존 UI 컴포넌트는 그대로 재사용하고, 서버 상태 연동을 위해 필요한 경우에도 가시적인 마크업/스타일 변경 없이 연결만 바꾼다.
- 예외는 이미 확정된 정책 범위 안에서의 상호작용 차단뿐이다.
  - 예: 대기/완료 상태에서 이동을 막고 `alert`를 띄우는 동작 변경
- 화면에 보이는 변경이 불가피하다고 판단되면 구현 전에 별도 확인이 필요하다.

### 포함

- 온보딩 API 타입, 클라이언트, React Query 훅 추가
- 인증 후 진입 경로 분기 수정
- 홈 퀘스트 배지 및 퀘스트 아이콘 동작 수정
- `/quest` 엔트리 라우트 및 서버 상태 기반 분기 추가
- 퀘스트 대기/완료/진행 화면의 API 연동
- 기존 로컬 퀘스트 판단 로직 제거 또는 축소
- 테스트 추가 및 회귀 검증

### 제외

- 신규 온보딩 완료 전용 화면 추가
- API 명세에 없는 신규 서버 필드 가정
- 퀘스트 외 다른 온보딩 기능 확장

## 4. 확정된 UX/정책

### 대기/완료 상태 카드 정책

- 기존 `src/features/quest/screens/QuestTemplateScreen.tsx`의 시각 구조를 그대로 사용한다.
- 완료된 퀘스트 카드와 다음 퀘스트 카드를 위아래로 노출한다.
- 카드 및 하단 액션 버튼 클릭 시 실제 이동은 막고 `alert`만 노출한다.

### alert 문구

- 완료된 퀘스트 카드 클릭: `이미 완료한 퀘스트예요.`
- 대기 중인 다음 퀘스트 카드 클릭: `다음 퀘스트는 아직 열리지 않았어요. 새로운 퀘스트가 열리면 알려드릴게요.`
- 대기/완료 상태 하단 액션 버튼 클릭: `현재 진행할 수 있는 퀘스트가 없어요. 새로운 퀘스트가 열리면 알려드릴게요.`

### API 실패 시 우선 UX

- `onboarding/status` 실패 시:
  - 홈은 정상 진입 유지
  - 퀘스트 배지는 숨김 처리
  - 퀘스트 아이콘 클릭은 허용하고 `/quest`에서 다시 조회 시도
- `onboarding/quests` 실패 시:
  - 퀘스트 화면에 오류 상태와 재시도 버튼 노출
  - 문구: `퀘스트 정보를 불러오지 못했어요. 다시 시도해주세요.`
  - 홈으로 강제 리다이렉트하지 않음

## 5. 태스크 분해

### Task 1. 온보딩 API 레이어 추가

#### 목표

- 온보딩 상태/퀘스트 조회를 위한 API 모듈, 타입, 훅을 추가한다.

#### 작업

- `/api/onboarding/status`, `/api/onboarding/quests` API 함수 추가
- 응답 타입, envelope 검증, 타입 가드 추가
- React Query 훅 및 쿼리 키 정의
- `apiClient` 인증 필요 경로에 온보딩 API 경로 추가

#### 제거/대체 대상

- 없음

#### 완료조건

- 화면에서 온보딩 상태와 퀘스트 목록을 안정적으로 조회할 수 있다.
- 비정상 응답과 API 실패에 대한 에러 처리가 기존 API 스타일과 동일하게 동작한다.

#### 리뷰 게이트

- 이 배치 수정이 끝나면 subagent에게 API 레이어 코드리뷰 요청
- 피드백 반영 후 다음 태스크 진행

### Task 2. 온보딩 상태 기반 파생 로직 정리

#### 목표

- 서버 응답만으로 진입, 배지, 진행 가능 퀘스트, 대기 상태를 판단할 수 있도록 파생 로직을 정리한다.

#### 작업

- 현재 오픈 퀘스트 단계 계산
- 첫 번째 퀘스트 완료 여부 계산
- 새 퀘스트 배지 노출 여부 계산
- 대기 상태/온보딩 완료 상태 판단 유틸 또는 훅 정리

#### 제거/대체 대상

- `homeSummary.closetCount` 기반 첫 퀘스트 완료 판단
- 화면마다 흩어진 하드코딩 상태 분기

#### 완료조건

- 홈, 진입 라우트, 퀘스트 화면이 동일한 기준으로 상태를 해석한다.

#### 리뷰 게이트

- 이 배치 수정이 끝나면 subagent에게 파생 상태/상태 해석 로직 리뷰 요청
- 피드백 반영 후 다음 태스크 진행

### Task 3. 인증 후 진입 경로와 홈 배지 연동

#### 목표

- 닉네임 저장 후 이동 경로와 홈 퀘스트 배지를 서버 상태 기준으로 맞춘다.

#### 작업

- `SetNicknameScreen` 성공 후 `/quest` 또는 `/home` 분기
- 홈 헤더 `hasNew.quest`를 `onboarding/status.hasNewQuest`에 연결
- 퀘스트 아이콘 진입 경로를 `/quest`로 통일

#### 제거/대체 대상

- 닉네임 저장 후 무조건 `/home`으로 이동하는 기존 로직
- 홈 헤더의 정적 `quest: false`
- 퀘스트 아이콘의 정적 `/quest/first` 이동

#### 완료조건

- 신규 사용자와 1차 퀘스트 완료 사용자의 진입 흐름이 스펙대로 분기된다.
- 홈에서 퀘스트 배지가 서버 상태와 일치한다.

#### 리뷰 게이트

- 이 배치 수정이 끝나면 subagent에게 진입 흐름/홈 연동 리뷰 요청
- 피드백 반영 후 다음 태스크 진행

### Task 4. `/quest` 엔트리 라우트와 상태 분기 추가

#### 목표

- 퀘스트 진입을 `/quest` 단일 엔트리로 통합하고 서버 상태 기반으로 분기한다.

#### 작업

- `/quest` 엔트리 라우트 추가
- 진행 가능 퀘스트가 있으면 해당 progress 화면으로 분기
- 대기 상태면 `QuestTemplateScreen` 기반 대기 화면으로 분기
- 온보딩 완료 후 퀘스트 아이콘 진입 시에도 동일한 카드 표기 정책 적용

#### 제거/대체 대상

- 정적 단계 진입을 전제로 한 라우팅 의존성

#### 완료조건

- 사용자는 항상 `/quest`만 진입하면 되고, 내부에서 올바른 상태 화면이 열린다.

#### 리뷰 게이트

- 이 배치 수정이 끝나면 subagent에게 라우팅/분기 로직 리뷰 요청
- 피드백 반영 후 다음 태스크 진행

### Task 5. 퀘스트 대기/완료 화면 상호작용 전환

#### 목표

- 기존 `QuestTemplateScreen`을 유지하면서 대기/완료 상태의 클릭 정책을 적용한다.

#### 작업

- 완료 퀘스트 카드 클릭 시 alert 연결
- 다음 퀘스트 카드 클릭 시 alert 연결
- 하단 액션 버튼 클릭 시 alert 연결
- 기존 화면의 카드 스타일과 레이아웃은 유지

#### 제거/대체 대상

- 대기/완료 상태에서도 실제 이동이 발생하는 기존 동작

#### 완료조건

- 대기 상태와 온보딩 완료 상태에서 지정된 alert 정책만 동작한다.

#### 리뷰 게이트

- 이 배치 수정이 끝나면 subagent에게 퀘스트 화면 상호작용 리뷰 요청
- 피드백 반영 후 다음 태스크 진행

### Task 6. 각 progress 화면의 API 기반 전환

#### 목표

- first/second/third progress 화면의 진행률과 완료 상태를 서버 응답 기반으로 전환한다.

#### 작업

- `requiredCount`, `currentCount`, `completed`를 API 응답으로 매핑
- 완료 화면 이동 조건을 서버 상태 기준으로 전환
- 로딩/에러/재시도 상태 처리

#### 제거/대체 대상

- `useQuestCategoryCount`
- `mockCount`, `mockComplete`
- `homeSummary`나 옷 목록 재계산으로 완료를 추정하는 로직

#### 완료조건

- progress 화면이 서버 응답만으로 정확한 진행률과 완료 여부를 표시한다.

#### 리뷰 게이트

- 이 배치 수정이 끝나면 subagent에게 progress 화면 API 연동 리뷰 요청
- 피드백 반영 후 다음 태스크 진행

### Task 7. 등록 완료 후 쿼리 무효화 정리

#### 목표

- 옷장 등록/옷 등록 후 홈과 온보딩 상태가 함께 최신화되도록 한다.

#### 작업

- 옷장 등록 성공 시 온보딩 관련 쿼리 invalidate 추가
- 옷 등록 성공 시 온보딩 관련 쿼리 invalidate 추가
- 필요 시 관련 화면 refetch 흐름 정리

#### 제거/대체 대상

- `home-summary`만 갱신하고 온보딩 상태는 남아 있는 기존 흐름

#### 완료조건

- 등록 직후 홈 배지, 퀘스트 진행률, 진입 상태가 최신 서버 값으로 갱신된다.

#### 리뷰 게이트

- 이 배치 수정이 끝나면 subagent에게 invalidate/refetch 흐름 리뷰 요청
- 피드백 반영 후 다음 태스크 진행

### Task 8. 테스트 및 회귀 검증

#### 목표

- API 파싱, 진입 분기, 배지, 대기 상태, 진행률 반영을 테스트로 고정한다.

#### 작업

- `onboarding/status` 정상/비정상 응답 테스트
- `onboarding/quests` 정상/비정상 응답 테스트
- 닉네임 저장 후 분기 테스트
- 홈 배지 표시 테스트
- 대기/완료 상태 alert 정책 테스트
- API 실패 시 오류 UI 테스트

#### 제거/대체 대상

- 없음

#### 완료조건

- 핵심 상태 전환과 회귀 위험 구간이 테스트로 보호된다.

#### 리뷰 게이트

- 이 배치 수정이 끝나면 subagent에게 테스트 범위/회귀 위험 리뷰 요청
- 피드백 반영 후 최종 마감

## 6. 제거 대상 요약

- `useQuestCategoryCount` 기반 퀘스트 진행률 계산
- `homeSummary.closetCount` 기반 첫 퀘스트 완료 추정
- `mockCount`, `mockComplete` 기반 목업 분기
- 서버 상태와 중복되는 하드코딩 퀘스트 완료/대기 판단
- 정적 `/quest/first` 중심 진입 흐름

## 7. 테스트 체크리스트

- 첫 번째 퀘스트 미완료 시 닉네임 저장 후 `/quest` 이동
- 첫 번째 퀘스트 완료 시 닉네임 저장 후 `/home` 이동
- `hasNewQuest=true`일 때 홈 퀘스트 배지 표시
- 대기 상태에서 완료 퀘스트 카드/다음 퀘스트 카드/하단 버튼 alert 동작
- 온보딩 완료 후 퀘스트 아이콘 진입 시 동일한 대기 화면 정책 적용
- `onboarding/status` 실패 시 홈 정상 진입 및 배지 숨김
- `onboarding/quests` 실패 시 재시도 UI 노출
- 옷장 등록/옷 등록 성공 후 온보딩 관련 쿼리 갱신
- progress 화면의 수치와 완료 상태가 서버 응답과 일치
