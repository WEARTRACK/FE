# 주간 옷장 회고/활용도 분석 구현 태스크 플랜

## 목적

`p1-spec-full.md`의 `4.1 주간 옷장 회고`와 `4.2 주간 옷장 활용도 분석`을 피그마 시안 기준으로 구현하고, `daily-reviews-api.md` 및 `weekly-reviews-api.md`의 API와 연결한다.

## 완성조건

- 홈 헤더의 주간회고 아이콘에서 `/weekly-review`로 진입해 오늘 입은 옷 선택 화면이 표시된다.
- 선택 저장 성공 시 `POST /api/daily-reviews/today` 응답을 반영해 주간 회고 결과 화면으로 이동한다.
- 결과 화면의 "이번 주 옷장 리포트" 버튼에서 옷장 활용도 분석 화면으로 이동한다.
- 활용률에 따라 `방치형/잠재형/활용형/마스터형 옷장` 텍스트, 범위, 컬러, 영수증 리포트가 달라진다.
- 영수증 스타일 리포트는 API 미완성 상태를 고려해 UI와 목 데이터 어댑터로 구현한다.
- 각 주요 작업 완료 후 subagent에게 코드 리뷰를 받고, 피드백을 반영한 뒤 다음 작업으로 넘어간다.
- 공통 토큰을 최대한 사용하며, 하드코딩이 불가피한 값은 구현 전에 사용자에게 항목과 이유를 알린다.

## 해야 할 범위

- `src/features/weekly-review/` 도메인 추가
- 주간 회고 선택/결과/활용도 분석/영수증 리포트 화면 추가
- `daily-reviews` 및 `weekly-reviews` API 연동
- 인증 필요 API 경로 추가
- 활용도 유형별 스타일/문구 매핑
- 하단 고정 버튼 영역 딤드/페이드 처리
- 영수증 3D 캐러셀 구현
- 타입/유틸/API 테스트 추가

## 공통 구현 원칙

- 색상, 폰트, spacing, radius는 기존 `src/constants/colors.ts`, NativeWind theme, 공통 컴포넌트 토큰을 우선 사용한다.
- 피그마 표현에 필요한 값이 공통 토큰에 없으면 기존 토큰으로 근사 가능한지 먼저 확인한다.
- 불가피한 하드코딩 값은 구현 전에 사용자에게 알리고, 승인 후 도메인 상수로 중앙화한다.
- 화면별 반복 UI는 `components/`로 분리하고, API 응답 모양은 `api/`와 `utils/`에서 매핑한다.
- 기존 사용자 변경 파일은 되돌리거나 정리하지 않는다.

## Task 1. API 기반 구조 만들기

### 작업

- `src/features/weekly-review/api/`를 생성한다.
- API envelope 공통 타입과 검증 유틸을 작성한다.
- `GET /api/daily-reviews/today` fetcher를 작성한다.
- `POST /api/daily-reviews/today` fetcher를 작성한다.
- `GET /api/weekly-reviews/current` fetcher를 작성한다.
- `src/lib/api/client.ts`의 인증 필요 경로에 `/api/daily-reviews`와 `/api/weekly-reviews`를 추가한다.

### 완료 기준

- API 응답이 문서와 다른 경우 `INVALID_RESPONSE` 계열 오류를 발생시킨다.
- 비즈니스 실패 envelope은 `ApiError`로 처리한다.
- 인증 토큰이 필요한 세 API에 Authorization 헤더가 붙는다.

### 리뷰

- Task 1 완료 후 subagent에게 API 응답 검증, 인증 경로, query key 설계를 리뷰받는다.
- 리뷰 피드백을 반영하거나, 반영하지 않는 경우 이유를 기록한다.

## Task 2. 도메인 타입/유틸 작성

### 작업

- `src/features/weekly-review/types/`를 생성한다.
- 회고 선택 옷, 카테고리, 주간 결과, 활용도 유형, 영수증 리포트 타입을 정의한다.
- 카테고리 정렬 유틸을 작성한다.
- 주차 라벨 유틸을 작성한다.
- 활용도 유형 판정 유틸을 작성한다.
- 착용률 clamp 유틸을 작성한다.

### 완료 기준

- 카테고리는 피그마/스펙 순서인 `T-shirt, Shirt, Knit, Hoodie, Vest, Cardigan, Pants, Shorts, Skirt, Dress, Jacket, Coat, Padding` 기준으로 노출된다.
- 활용도 유형 경계값은 다음 기준을 따른다.
  - `0~20`: 방치형 옷장
  - `21~50`: 잠재형 옷장
  - `51~80`: 활용형 옷장
  - `81~100`: 마스터형 옷장
- UI 표시에 쓰는 활용률은 `0~100` 범위로 고정된다.

### 리뷰

- Task 2 완료 후 subagent에게 경계값, 카테고리 정렬, 타입 확장성을 리뷰받는다.
- 리뷰 피드백을 반영하거나, 반영하지 않는 경우 이유를 기록한다.

## Task 3. Query/Mutation 훅 작성

### 작업

- `src/features/weekly-review/hooks/`를 생성한다.
- 오늘 회고 선택 화면 조회 훅을 작성한다.
- 오늘 회고 저장 mutation 훅을 작성한다.
- 현재 주차 회고 결과 조회 훅을 작성한다.
- 저장 성공 시 관련 query cache를 갱신한다.

### 완료 기준

- `/weekly-review` 진입 시 `GET /api/daily-reviews/today`를 호출한다.
- 저장 성공 시 `POST /api/daily-reviews/today` 응답을 결과 화면 데이터로 사용할 수 있다.
- 결과/분석 화면 직접 진입 시 `GET /api/weekly-reviews/current`로 데이터를 복구할 수 있다.

### 리뷰

- Task 3 완료 후 subagent에게 cache 갱신, loading/error 상태, 직접 진입 복구 흐름을 리뷰받는다.
- 리뷰 피드백을 반영하거나, 반영하지 않는 경우 이유를 기록한다.

## Task 4. 라우트 구성

### 작업

- 기존 `app/(main)/weekly-review.tsx`를 선택 화면 엔트리로 교체한다.
- `app/(main)/weekly-review/result.tsx`를 추가한다.
- `app/(main)/weekly-review/analysis.tsx`를 추가한다.
- `app/(main)/weekly-review/receipt.tsx`를 추가한다.

### 완료 기준

- 홈 헤더 주간회고 아이콘에서 선택 화면으로 진입한다.
- 저장 성공 후 결과 화면으로 이동한다.
- 결과 화면에서 분석 화면으로 이동한다.
- 분석 화면에서 영수증 리포트 화면으로 이동한다.
- 각 화면의 back 동작이 기존 앱 패턴과 일치한다.

### 리뷰

- Task 4 완료 후 subagent에게 Expo Router 경로, 뒤로가기 정책, 직접 진입 케이스를 리뷰받는다.
- 리뷰 피드백을 반영하거나, 반영하지 않는 경우 이유를 기록한다.

## Task 5. 주간 회고 선택 화면 구현

### 작업

- 선택 화면 `WeeklyReviewSelectionScreen`을 작성한다.
- 카테고리별 옷 그리드 컴포넌트를 작성한다.
- 체크 가능한 옷 카드 컴포넌트를 작성한다.
- 선택 개수 표시를 구현한다.
- 하단 고정 저장 버튼과 딤드/페이드 오버레이를 구현한다.
- 등록된 옷 없음, 조회 실패, 로딩 상태를 구현한다.

### 완료 기준

- 등록된 전체 옷 목록이 카테고리별로 표시된다.
- 카테고리별 옷은 기본 3열 x 최대 2행으로 표시된다.
- 6개 초과 시 가로 스크롤과 인디케이터가 표시된다.
- 다중 선택/선택 해제가 가능하다.
- 0벌 저장 시 "이번 주 착용한 옷이 없으신가요?" 토스트 후 저장을 허용한다.
- 등록된 옷 없음 시 "등록된 옷이 없습니다. 먼저 옷을 등록해주세요" 토스트를 표시한다.

### 리뷰

- Task 5 완료 후 subagent에게 선택 상태, 스크롤/딤드 처리, 빈 상태/토스트를 리뷰받는다.
- 리뷰 피드백을 반영하거나, 반영하지 않는 경우 이유를 기록한다.

## Task 6. 주간 회고 결과 화면 구현

### 작업

- 결과 화면 `WeeklyReviewResultScreen`을 작성한다.
- 기록 완료 카드, 월/주차 요약, 입은 옷 수/활용률 카드, 인사이트 카드, 이번 주 입은 옷 목록을 구현한다.
- "이번 주 옷장 리포트" 버튼을 구현한다.
- 저장 직후 전달 데이터와 직접 진입 조회 데이터를 모두 처리한다.

### 완료 기준

- `POST /api/daily-reviews/today` 응답으로 결과 화면이 즉시 표시된다.
- 직접 진입 시 `GET /api/weekly-reviews/current` 결과로 화면이 표시된다.
- 인사이트 문구는 API의 `weeklyInsight`를 사용한다.
- 입은 옷 목록은 카테고리 정렬과 그리드 규칙을 따른다.

### 리뷰

- Task 6 완료 후 subagent에게 저장 후 이동 흐름, 데이터 fallback, 결과 UI 상태를 리뷰받는다.
- 리뷰 피드백을 반영하거나, 반영하지 않는 경우 이유를 기록한다.

## Task 7. 활용도 분석 화면 구현

### 작업

- 분석 화면 `WeeklyReviewAnalysisScreen`을 작성한다.
- `react-native-svg` 기반 도넛 차트를 구현한다.
- 활용도 유형 카드 컴포넌트를 작성한다.
- 유형별 텍스트, 범위, 컬러를 도메인 상수로 관리한다.
- `00형 옷장 >` 클릭 시 영수증 리포트 화면으로 이동한다.

### 완료 기준

- 활용률이 도넛 차트 중앙에 표시된다.
- 유형별 범위와 이름이 정확히 표시된다.
- 보조 문구는 "입지 않는 옷이 N벌 있어요!" 형식으로 표시된다.
- 등록된 옷이 없거나 회고 미완료 상태면 스펙에 맞게 토스트/대체 표시를 한다.
- 유형별 색상은 공통 토큰 우선, 불가피한 경우 사전 고지 후 도메인 상수로 반영한다.

### 리뷰

- Task 7 완료 후 subagent에게 도넛 차트 계산, 유형 경계값, 토큰 사용 여부를 리뷰받는다.
- 리뷰 피드백을 반영하거나, 반영하지 않는 경우 이유를 기록한다.

## Task 8. 영수증 스타일 리포트 구현

### 작업

- `react-native-reanimated-carousel` 의존성을 추가한다.
- 영수증 화면 `WeeklyReviewReceiptScreen`을 작성한다.
- 영수증 목 데이터 어댑터를 작성한다.
- 유형명, 활용률, 이번 주 입은 옷 캐러셀, `N벌 TOTAL PRICE`, 총액 영역을 구현한다.
- 캐러셀 scale/opacity 기반 3D 효과를 구현한다.

### 완료 기준

- 방치형/잠재형/활용형/마스터형에 따라 영수증 텍스트와 컬러가 달라진다.
- 영수증 API 미완성 상태에서도 목 데이터로 화면을 확인할 수 있다.
- 실제 API가 완성되면 어댑터/fetcher만 교체할 수 있다.
- 캐러셀 중앙 카드는 크게, 양옆 카드는 작고 흐리게 보인다.

### 리뷰

- Task 8 완료 후 subagent에게 목 어댑터 분리, 캐러셀 동작, 유형별 UI 차이를 리뷰받는다.
- 리뷰 피드백을 반영하거나, 반영하지 않는 경우 이유를 기록한다.

## Task 9. 테스트 작성

### 작업

- API fetcher 테스트를 작성한다.
- 타입 guard 테스트를 작성한다.
- 카테고리 정렬 테스트를 작성한다.
- 활용도 유형 경계값 테스트를 작성한다.
- 착용률 clamp 테스트를 작성한다.

### 완료 기준

- envelope 실패 시 `ApiError`가 발생한다.
- malformed result 시 `INVALID_RESPONSE`가 발생한다.
- 활용도 유형 경계값 `0, 20, 21, 50, 51, 80, 81, 100`이 기대 유형으로 판정된다.
- `weeklyClosetUsageRate`가 `0~100` 범위로 표시된다.

### 리뷰

- Task 9 완료 후 subagent에게 테스트 누락, 경계값, 회귀 위험을 리뷰받는다.
- 리뷰 피드백을 반영하거나, 반영하지 않는 경우 이유를 기록한다.

## Task 10. 최종 검증

### 작업

- `npm run typecheck`를 실행한다.
- `npm run lint`를 실행한다.
- `npm run test`를 실행한다.
- 앱에서 주요 플로우를 수동 확인한다.

### 수동 확인 시나리오

- 주간회고 진입
- 다중 선택/선택 해제
- 0벌 저장
- 저장 후 결과 화면 이동
- 등록 옷 없음 상태
- 조회 실패/저장 실패 상태
- 하단 버튼 영역 딤드 처리
- 결과 화면에서 분석 화면 이동
- 분석 화면에서 영수증 화면 이동
- 방치형/잠재형/활용형/마스터형별 텍스트와 컬러 차이
- 영수증 캐러셀 scale/opacity 효과

### 리뷰

- Task 10 완료 후 subagent에게 최종 코드 리뷰를 요청한다.
- 최종 피드백 반영 후 검증 명령을 다시 실행한다.

## 사용자 결정이 필요한 항목

- 영수증 실제 API 응답 계약이 완성되면 공유가 필요하다.
- 특히 총액, 착용 옷 카드 목록, 유형별 문구/컬러를 서버가 줄지 프론트가 계산할지 결정이 필요하다.
- 구현 중 공통 토큰에 없는 피그마 색상/치수/그림자가 확인되면, 해당 값은 별도 승인 후 상수화해서 반영한다.
